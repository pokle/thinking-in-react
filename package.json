{
  "name": "react-start",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p public/build && browserify -t [ babelify --presets [ react ] ] src/app.js -o public/build/bundle.js",
    "watch": "mkdir -p public/build &&   watchify -t [ babelify --presets [ react ] ] src/app.js -o public/build/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  }
}
